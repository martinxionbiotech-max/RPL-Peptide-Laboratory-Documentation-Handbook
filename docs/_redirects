# RPL Peptide Docs - Cloudflare Pages redirect rules
# MkDocs generates /{path}/index.html → Cloudflare auto-serves with trailing slash

# Root redirect (no trailing slash)
/ /index.html 200

# Block all other catch-all patterns — Cloudflare auto-routes correctly
# DO NOT add `/* /index.html` — it breaks every product page!
